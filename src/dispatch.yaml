dispatch:
  - url: "*/favicon\.ico"
    module: default

  - url: "*/stylesheets/*"
    module: default

  - url: "*/static/*"
    module: default

  # Go used for just the API portion
  - url: "*/api*"
    module: goapi

  # Default module serves everything else
  - url: "*/*"
    module: pythonapp
